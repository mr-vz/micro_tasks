import {Navigate} from "react-router-dom";
import * as React from "react";

export const MicroTasks = () => {
    return (
            <>
                {<Navigate to={'/adidas'}/>}
            </>
            )
};